document.addEventListener('DOMContentLoaded',()=>{const nav=[...document.querySelectorAll('.header nav a')];const sections=[...document.querySelectorAll('main section[id]')];const menu=document.querySelector('.menu');const links=document.querySelector('.header nav');menu?.addEventListener('click',()=>{links.style.display=links.style.display==='flex'?'none':'flex';links.style.position='absolute';links.style.top='70px';links.style.left='0';links.style.right='0';links.style.padding='20px';links.style.background='#090a0a';links.style.flexDirection='column'});const observer=new IntersectionObserver(es=>{es.forEach(e=>{if(e.isIntersecting){nav.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+e.target.id))}})},{rootMargin:'-35% 0px -55%'});sections.forEach(s=>observer.observe(s));document.querySelectorAll('a[href="#"]').forEach(a=>a.addEventListener('click',e=>e.preventDefault()));});